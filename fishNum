#include<iostream>
using namespace std;
int main()
{
	int n = 0, arr[101],temp;
	do
	{
		cin >> temp;
		arr[n] = temp;
		n++;
	} while (temp != 0);
	n--;
	while (n--)
		cout << arr[n] << ' ';
	return 0;
}
